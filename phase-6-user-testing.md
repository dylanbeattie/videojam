Here's the things that are wrong with phase 6:

1. The performer video never plays. (All we see is the audience video) - this is a regression; this was working in a previous phase.
2. The always-on-top video obscures the application's main window, so we can't stop, control, interact with it at all if a video frame or fallback screen is displayed
3. There's no toolbar. We didn't ask for one but it turns out having a Play/Pause/Stop/Rewind toolbar in the UI is actually a big deal.

